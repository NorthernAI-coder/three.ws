# ── TripoSR (VAST-AI / Stability AI) ──────────────────────────────────────────
# The `tsr` package is NOT pip-installable (no setup.py/pyproject); the Dockerfile
# clones the repo to /opt/triposr and puts it on PYTHONPATH instead. Its own deps
# (incl. the CUDA-compiled torchmcubes) come from /opt/triposr/requirements.txt.

torch>=2.3.0
trimesh>=4.4.0
Pillow>=10.3.0

# Background removal (TripoSR works best with isolated subject).
rembg[gpu]==2.0.56

# ── Server ────────────────────────────────────────────────────────────────────
fastapi==0.115.0
uvicorn[standard]==0.32.0
pydantic>=2.0.0
httpx>=0.27.0

# ── GCP ───────────────────────────────────────────────────────────────────────
google-cloud-storage==2.18.0
