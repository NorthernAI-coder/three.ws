# Worker-level deps. MDM's own requirements (clip, spacy, smplx, chumpy, …) are
# installed from its cloned repo in the Dockerfile, mirroring how model-triposr
# installs TripoSR's requirements.
fastapi==0.115.0
uvicorn[standard]==0.32.0
pydantic>=2.0.0
numpy>=1.24.0
google-cloud-storage==2.18.0
