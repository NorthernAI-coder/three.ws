# ── LingBot-Map (Robbyant/lingbot-map, Apache-2.0) ────────────────────────────
# The package + its own deps are installed from the cloned repo in the Dockerfile
# (pip install -e /opt/lingbot-map). torch/torchvision are pinned in the Dockerfile
# to match the CUDA 12.8 base. These are the extra runtime deps this worker needs
# on top of LingBot-Map's own requirements.

numpy>=1.26.0
opencv-python-headless>=4.9.0

# ── Server ────────────────────────────────────────────────────────────────────
fastapi==0.115.0
uvicorn[standard]==0.32.0
pydantic>=2.0.0
httpx>=0.27.0

# ── GCP ───────────────────────────────────────────────────────────────────────
google-cloud-storage==2.18.0
