{
	"name": "@three-ws/liquidation-collector",
	"version": "1.0.0",
	"private": true,
	"type": "module",
	"description": "Standalone collector for public Binance/Bybit/OKX futures liquidation WebSocket streams — serves an aggregate REST snapshot consumed by api/coin/liquidations.js.",
	"main": "src/index.js",
	"scripts": {
		"start": "node src/index.js",
		"dev": "node --watch src/index.js"
	},
	"engines": {
		"node": ">=20"
	},
	"dependencies": {
		"ws": "^8.21.0"
	}
}
