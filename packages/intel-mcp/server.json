{
	"$schema": "https://static.modelcontextprotocol.io/schemas/2025-12-11/server.schema.json",
	"name": "io.github.nirholas/intel-mcp",
	"title": "three.ws Intel",
	"description": "Read the market the way the smart money does — coin smart-money scores, wallet reputation, signal feeds, KOL leaderboards/trades, and the copy-trade Smart Money directory.",
	"websiteUrl": "https://three.ws",
	"icons": [
		{
			"src": "https://three.ws/three-ws-mcp-icon.svg",
			"mimeType": "image/svg+xml",
			"sizes": [
				"any"
			]
		}
	],
	"repository": {
		"url": "https://github.com/nirholas/three.ws",
		"source": "github",
		"subfolder": "packages/intel-mcp"
	},
	"version": "0.1.0",
	"packages": [
		{
			"registryType": "npm",
			"identifier": "@three-ws/intel-mcp",
			"version": "0.1.0",
			"runtimeHint": "npx",
			"transport": {
				"type": "stdio"
			},
			"environmentVariables": [
				{
					"name": "THREE_WS_BASE",
					"description": "Base URL of the three.ws API that serves the intel endpoints. Override only when self-hosting or targeting a preview deployment.",
					"format": "string",
					"isRequired": false,
					"default": "https://three.ws"
				},
				{
					"name": "THREE_WS_TIMEOUT_MS",
					"description": "Per-request timeout in milliseconds for the live intel reads.",
					"format": "number",
					"isRequired": false,
					"default": "20000"
				}
			]
		}
	]
}
