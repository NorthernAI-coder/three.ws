{
	"$schema": "https://static.modelcontextprotocol.io/schemas/2025-12-11/server.schema.json",
	"name": "io.github.nirholas/vanity-mcp",
	"title": "three.ws Vanity",
	"description": "Read the three.ws vanity-address grind-bounty market — quote a pattern's difficulty + USDC price, appraise any address's rarity, and browse the board, claimable queue, leaderboard, and proof-of-grind gallery.",
	"websiteUrl": "https://three.ws",
	"icons": [
		{
			"src": "https://three.ws/three-ws-mcp-icon.svg",
			"mimeType": "image/svg+xml",
			"sizes": [
				"any"
			]
		}
	],
	"repository": {
		"url": "https://github.com/nirholas/three.ws",
		"source": "github",
		"subfolder": "packages/vanity-mcp"
	},
	"version": "0.1.0",
	"packages": [
		{
			"registryType": "npm",
			"identifier": "@three-ws/vanity-mcp",
			"version": "0.1.0",
			"runtimeHint": "npx",
			"transport": {
				"type": "stdio"
			},
			"environmentVariables": [
				{
					"name": "THREE_WS_BASE",
					"description": "Base URL of the three.ws API that serves /api/vanity. Override only when self-hosting or targeting a preview deployment.",
					"format": "string",
					"isRequired": false,
					"default": "https://three.ws"
				},
				{
					"name": "THREE_WS_TIMEOUT_MS",
					"description": "Per-request timeout in milliseconds. All endpoints are reads, so the default is brisk with headroom for a cold cache.",
					"format": "number",
					"isRequired": false,
					"default": "20000"
				}
			]
		}
	]
}
