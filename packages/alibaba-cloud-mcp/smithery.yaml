# Smithery deployment config for @three-ws/alibaba-cloud-mcp
# https://smithery.ai/docs/config#smitheryyaml
startCommand:
  type: stdio
  configSchema:
    type: object
    properties: {}
    required: []
  commandFunction: |
    (config) => ({ command: "npx", args: ["-y", "@three-ws/alibaba-cloud-mcp"], env: {} })
