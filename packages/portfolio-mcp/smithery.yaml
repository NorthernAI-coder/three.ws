# Smithery deployment config for @three-ws/portfolio-mcp
# https://smithery.ai/docs/config#smitheryyaml
startCommand:
  type: stdio
  configSchema:
    type: object
    properties: {}
    required: []
  commandFunction: |
    (config) => ({ command: "npx", args: ["-y", "@three-ws/portfolio-mcp"], env: {} })
